{
  "name": "veyra",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present"
  },
  "dependencies": {
    "@altananetwork/sdk": "^0.9.0"
  }
}
